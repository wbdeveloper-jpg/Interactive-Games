# Optional Audio

Place sound files in this folder if desired, then assign them to
`MeasurementAudioManager` under the generated current-scene UI root.

Available Inspector slots include background music, button, correct, wrong,
Hint, weight drop, water and timeout sounds.

The game has no mandatory audio-file dependency.
